## 0.0.2

- Format json outputs for quiet flag
- Remove serve logRequest method

## 0.0.1

- Initial version: stream and control booted iOS simulators via the CLI or a
  local web preview server, with `tap`, `type`, `button`, `gesture`, and
  `rotate` control commands.
