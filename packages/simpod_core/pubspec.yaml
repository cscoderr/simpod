name: simpod_core
description:
  Shared protocol models for simpod - sessions, devices, accessibility
  nodes, orientations, and settings used by the simpod CLI and web client.
version: 0.0.2
repository: https://github.com/cscoderr/simpod

environment:
  sdk: ^3.12.0
resolution: workspace

# Add regular dependencies here.
# dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
